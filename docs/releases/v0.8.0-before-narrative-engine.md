# v0.8.0-before-narrative-engine

Date: 2026-06-26

Baseline name: `backup_before_narrative_engine_2026_06_26`

Purpose: stable checkpoint before starting the Managed Narrative Business Objects architecture.

## Scope

- Current application state is preserved before narrative-engine architecture work begins.
- Executive Report tabbed navigation is baselined with these chapters: Overview, Decisions, Risks, Workstreams / Milestones, Gantt, Narrative.
- Decisions remain a separate chapter from narrative content.
- Workstreams and Milestones are grouped into one operational chapter.
- Existing project operations data remains in the local SQLite database; no reset, seed, or destructive migration was run.

## Validation

- `npm run lint`: passed.
- `npm run check:i18n-config`: passed.
- `npx tsc --noEmit`: passed after regenerating `.next/types` through the production build.
- `$env:NODE_OPTIONS='--use-system-ca'; npm run build`: passed. The `NODE_OPTIONS` setting is required in this environment so `next/font` can verify the system certificate chain while fetching Google font assets.
- `npm run start -- --port 3010`: verified through a controlled local harness.

## Runtime Checks

- `/executive-report`: returned HTTP 200.
- `/executive-report?chapter=decisions`: returned HTTP 200 and rendered Decision Cockpit content.
- `/executive-report?chapter=workstreams`: returned HTTP 200 and rendered Workstreams and Milestones content.
- Spanish locale cookie `project_ops_locale=es`: returned HTTP 200 and rendered Spanish labels, confirming English/Spanish switching remains functional.

## Data Preservation Snapshot

Read-only counts from `dev.db`:

- `Project`: 5
- `ProjectWorkstream`: 72
- `ProjectEvent`: 17
- `ProjectRisk`: 5
- `ProjectDecision`: 2
- `ProjectReportingPack`: 2
- `ProjectTask`: 9
- `TimeEntry`: 37
- `Organization`: 2

## Notes

- The baseline includes the root locale initialization fix so the application honors the persisted locale cookie across reloads.
- Local runtime logs and generated build output are not part of the baseline commit.
